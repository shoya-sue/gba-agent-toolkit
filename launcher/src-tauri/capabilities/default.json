{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "GBA Agent Launcher の既定 capability（core のみ・カスタムコマンドは invoke_handler で許可）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
